# TXM Crack Detection — runtime dependencies
numpy>=1.22
scipy>=1.9
scikit-learn>=1.9        # The shipped hybrid was pickled by 1.9.0, so 1.9 is a hard
# floor for it -- sklearn gives no cross-version unpickling guarantee, and an older
# runtime may fail or, worse, load it wrong. The two older models here are 1.7.2
# pickles; loading those under 1.9 warns, and the warning was checked rather than
# assumed: max probability delta 5.5e-6 over the reference frames, zero class flips.
                         # sklearn loads them with an InconsistentVersionWarning that
                         # says results may be invalid; MEASURED on 20,000 pixels,
                         # 1.7.2 vs 1.9.0 differ by at most 5.5e-6 in probability and
                         # flip ZERO pixels at thresholds 0.2/0.5/0.8. So the warning
                         # is expected and harmless here -- not silenced, because the
                         # day it stops being harmless is the day you want to see it.
scikit-image>=0.20
tifffile>=2022.5
imagecodecs>=2022.2      # NOT optional, despite tifffile treating it as an extra. All 71
# shipped images are float32 TIFF with the floating-point predictor (predictor 3), and
# tifffile refuses those without this package:
#     ValueError: <PREDICTOR.FLOATINGPOINT: 3> requires the 'imagecodecs' package
# Nothing pulls it in transitively -- `pip show imagecodecs` reports "Required-by: (nothing)"
# -- so a clone that installed only what this file used to list could not read a single one
# of its own images. It was missing here for as long as the images have been in this format;
# every development machine had it installed by hand, which is exactly why nobody noticed.
# Caught by the Linux CI added alongside this line.
Pillow>=9.0
joblib>=1.2
Flask>=2.2
matplotlib>=3.6          # figures / diagnostics only

# SAM. torch is the large one; CPU-only wheels are fine, a GPU just makes it faster.
torch>=2.0
transformers>=4.40
torchvision>=0.15         # Not imported anywhere in this project, and still required, because
# transformers picks its image-processor backend by what is INSTALLED, not by what you ask
# for: with torchvision present SamProcessor returns SamImageProcessor, without it
# SamImageProcessorPil. Both were measured on the reference frame and agree -- 9 flipped
# pixels out of 2,857,784, predicted area 0.187970 against 0.187972 -- so this pin is not
# fixing a bug. It is removing a silent fork in the environment. Every development machine
# here had torchvision installed by hand while requirements.txt did not ask for it, so CI ran
# a different preprocessing path than development did, and nobody could have known from this
# file. Same class of problem as the imagecodecs line above, caught the same way.
